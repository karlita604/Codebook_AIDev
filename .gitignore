data/repo_cache/
data/tool_output/
data/snapshots/
data/archive/
.venv/
__pycache__/
logs/
# .NET build artifacts (src/inhouse/roslyn_tool/) - same reasoning as
# __pycache__/: compiled output, not source, regenerated by `dotnet build`.
bin/
obj/

# GITHUB_TOKENS_FILE (src/phase0/pr_sampling_pipeline.py) - additional
# GitHub PATs for round-robin, never committed. Keep the real file outside
# the repo (e.g. the user's own machine) if possible; this pattern is a
# backstop in case one ends up here anyway.
github_tokens*
.github_tokens
